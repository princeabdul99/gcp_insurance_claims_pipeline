
models:
  
  - name: fact_claims_decision
    description: Claims decision data that combines the claims announcement, claims payment, claims reserves and policy table. one row per claim
    columns:
      
      - name: claim_id
        description: The unique key for each claim decision.
        data_type: int64
        data_tests:
          - not_null
      
      # - name: policy_id
      #   description: The unique key for each claim decision.
      #   data_type: int64
      #   data_tests:
      #     - relationships:
      #         arguments: 
      #           to: ref('dim_policies')
      #           field: policy_id             


